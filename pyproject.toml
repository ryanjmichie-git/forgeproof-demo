[project]
name = "forgeproof-demo"
version = "0.1.0"
description = "Demo project for ForgeProof provenance-tracked development"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["."]

[tool.ruff]
line-length = 100
